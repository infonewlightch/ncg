import {useEffect,useState} from 'react';
import {ArrowLeft,ArrowRight,BookOpen,Check,CheckCircle,Eye,Search} from 'lucide-react';
import {useApp} from '../state';
import {Modal} from '../components/Ui';
import {contentLanguage,baseLanguage,languageName} from '../core/languages';
import {completeLesson} from '../core/learning';

type CatechismItem={n:number;q:string;a:string};
const groups=[{id:'all',from:1,to:107,ko:'전체',en:'All'},{id:'faith',from:1,to:38,ko:'하나님과 구원',en:'God & salvation'},{id:'life',from:39,to:84,ko:'십계명과 삶',en:'Commandments & life'},{id:'grace',from:85,to:97,ko:'은혜의 방편',en:'Means of grace'},{id:'prayer',from:98,to:107,ko:'기도',en:'Prayer'}];
export default function Catechism(){
 const {state,t,update}=useApp();const [items,setItems]=useState<CatechismItem[]>([]);const [error,setError]=useState(false);const [query,setQuery]=useState('');const [group,setGroup]=useState('all');const [card,setCard]=useState<number|null>(null);const [revealed,setRevealed]=useState(false);const language=contentLanguage(state.language,['ko','en','th']);
 useEffect(()=>{let alive=true;const loaders={ko:()=>import('../data/catechism/ko.json'),en:()=>import('../data/catechism/en.json'),th:()=>import('../data/catechism/th.json')};setItems([]);setCard(null);setError(false);loaders[language as keyof typeof loaders]().then(data=>{if(alive)setItems(data.default);}).catch(()=>{if(alive)setError(true);});return()=>{alive=false;};},[language]);
 const scope=groups.find(g=>g.id===group)!;const list=items.filter(x=>x.n>=scope.from&&x.n<=scope.to&&`${x.n} ${x.q} ${x.a}`.toLocaleLowerCase().includes(query.toLocaleLowerCase()));
 const done=items.filter(x=>state.completed.includes(`catechism:${x.n}`)).length;const current=items.find(x=>x.n===card);const isDone=current&&state.completed.includes(`catechism:${current.n}`);
 function open(n:number){setCard(n);setRevealed(false);}
 function complete(){if(!current)return;update(s=>({...s,completed:completeLesson(s.completed,`catechism:${current.n}`)}));}
 return <><a className="text-link back-link" href="#/school"><ArrowLeft size={17}/>{t('성경교육','Bible School')}</a><div className="school-heading catechism-heading"><div><span className="eyebrow">{t("NCG SCHOOL · 소요리문답","NCG SCHOOL \u00b7 CATECHISM")}</span><h1>{t('웨스트민스터 소요리문답','Westminster Shorter Catechism')}</h1><p>{t('107개의 문답으로 차근차근 배우는 믿음의 기초.','Explore the foundations of faith through 107 questions and answers.')}</p></div><BookOpen size={56} strokeWidth={1}/></div><div className="catechism-progress"><span><b>{done}</b> / 107 {t('문답 학습','studied')}</span><progress value={done} max={107} aria-label={t('문답 학습 진도','Catechism progress')}/><button className="button" disabled={!items.length} onClick={()=>open(items.find(x=>!state.completed.includes(`catechism:${x.n}`))?.n||1)}>{done?t('이어서 배우기','Continue learning'):t('첫 문답 시작','Start learning')}<ArrowRight size={17}/></button></div>{language!==baseLanguage(state.language)&&<p className="callout">{t('선택한 언어의 번역을 준비 중입니다. 현재 교육 언어:','Translation is being prepared. Current lesson language:')} {languageName(language,state.ui)}</p>}<label className="search-box"><Search size={18}/><input aria-label={t("문답 검색","Search catechism")} value={query} onChange={e=>setQuery(e.target.value)} placeholder={t('문답 번호나 내용 검색','Search by number or content')}/></label><div className="tabs catechism-tabs">{groups.map(g=><button key={g.id} className={group===g.id?'active':''} onClick={()=>setGroup(g.id)}>{t(g.ko,g.en)}</button>)}</div>{error?<p className="error" role="alert">{t('문답을 불러오지 못했습니다. 다시 접속해주세요.','Unable to load the catechism. Please reload.')}</p>:items.length?<div className="catechism-list">{list.map(x=><button key={x.n} onClick={()=>open(x.n)}><span className="catechism-number">{state.completed.includes(`catechism:${x.n}`)?<Check size={20}/>:String(x.n).padStart(2,'0')}</span><b lang={language}>{x.q}</b><ArrowRight size={17}/></button>)}{!list.length&&<p>{t('일치하는 문답이 없습니다.','No matching questions.')}</p>}</div>:<p>{t('문답을 불러오는 중…','Loading questions…')}</p>}<p className="library-note">{t('다바르의 문답과 번역을 이어받았습니다. 공개 운영을 위한 교회 검수 중입니다.','Adapted from the owner’s Dabar content. Church review for public use is pending.')}</p>{current&&<Modal wide title={t('제{number}문','Question {number}').replace('{number}',String(current.n))} onClose={()=>setCard(null)}><article className="catechism-card" lang={language}><span className="eyebrow">{t("웨스트민스터 소요리문답","WESTMINSTER SHORTER CATECHISM")} · {current.n} / 107</span><h2>{current.q}</h2>{revealed?<div className="catechism-answer"><span className="eyebrow">{t('답','ANSWER')}</span><p>{current.a}</p></div>:<button className="reveal-answer" onClick={()=>setRevealed(true)}><Eye size={21}/>{t('생각해 본 뒤, 답 펼치기','Think it through, then reveal the answer')}</button>}</article><div className="catechism-card-actions"><button className="text-link" disabled={current.n===1} onClick={()=>open(current.n-1)}><ArrowLeft size={16}/>{t('이전','Previous')}</button><button className="button" disabled={!revealed||Boolean(isDone)} onClick={complete}>{isDone?<CheckCircle size={17}/>:<Check size={17}/>} {isDone?t('학습했어요','Studied'):t('이 문답을 학습했어요','Mark as studied')}</button><button className="text-link" disabled={current.n===107} onClick={()=>open(current.n+1)}>{t('다음','Next')}<ArrowRight size={16}/></button></div></Modal>}</>;
}
