# Interface localization

2026-09-24: NCG now retains the selected BCP 47 language/script for its interface, instead of coercing every language other than Korean, English and Spanish to English. Korean/English/Spanish ship with the app; generated packs for Portuguese, Simplified Chinese, Traditional Chinese, Thai, Arabic, Persian, Hindi and French also load on demand without a model request. Other written languages request automatic translations of the current page's known UI messages. This is a delivery mechanism, not a claim that all ISO entries (including sign languages) have verified translations.

- `src/i18n/source.json` is the developer-owned catalogue (768 messages). Browser requests contain only language, fixed batch number and catalogue revision. They cannot include user posts, chat, credentials or arbitrary text. Scripture and course content are not sent to this endpoint.
- `/api/interface-translation` uses the existing Netlify AI Gateway. It checks origin, JSON size, exact language/script, source revision, message IDs, order, placeholders/links, output size and completion status. The model must decline languages it cannot translate reliably. Sign languages cannot be represented as text UI translations and receive an explicit unavailable state. Generated translations are marked unreviewed and require native-speaker review.
- Cache keys include revision, model, language/script and batch. Validated results are shared in Netlify Blobs and saved on the device for reuse. Failed batches do not automatically retry or turn into a success state. Changing the language does not apply late results from the previous language.
- Requests are bounded: 24 fixed messages, model timeout 25 seconds, 6,000 output tokens, two browser requests and four model requests per process at once. Netlify rate limit: 80 requests per 180 seconds per IP/domain. UI generation has a separate shared Blobs counter of 300 uncached requests per UTC day. Conditional writes, failed PUT rejection and required read ETags protect the counter, but this is not a financial spending guarantee. Existing Bible/community PostgreSQL budgets are unchanged. No payment plan, auto-recharge, secret or database migration was changed.
- RTL script detection uses the selected BCP 47 script; desktop sidebar placement follows direction. Source Bible passages retain their edition language and direction.

Validation pending deployment: unit tests cover translation input boundaries, cache reuse, unsupported output, late-language responses, placeholders, script direction and concurrent budget claims. Production model delivery and native-language accuracy must be recorded separately after verification.

Production verification: Portuguese generated successfully for all 768 catalogue messages in 32 batches. Public browser menu, reader controls, status, page heading and persisted language confirmed in Portuguese. Generated Portuguese source is `src/i18n-generated/pt.json`, marked unreviewed; it is not a native-speaker reviewed translation. Public static asset SHA-256 matched the local production build.

Additional checks: live Arabic, French and Traditional Chinese (`zh-Hant`) navigation batches returned HTTP 200 in their requested script. Arabic public browser showed translated home and Bible controls with RTL desktop sidebar; a 390px mobile home in normal and large-text modes had document width 390px and no horizontal overflow. Portuguese Genesis 1 and 2 rendered the Biblica source, with 66 books and verse 1 selected after changing chapters. Final tests: 309 across 59 files; production build and 768-key catalogue check passed. These are delivery/layout tests, not expert linguistic or theological review. QT reflection guides and educational lesson bodies use their separately authored content translations and are not silently rewritten by the UI endpoint.

## Pregenerated interface packs — 2026-09-24 13:35 KST

- Each of the eight generated language/script packs contains all 768 current UI messages (6,144 total). They are separate lazy-loaded production assets, not an all-language download on every visit. Existing English/Korean/Spanish UI dictionaries are unchanged.
- Persian `fa-IR` and Hindi `hi-IN` use their matching parent packs. Chinese variants select Simplified or Traditional by BCP 47 script; e.g. `zh-CN`/`zh-SG` use Simplified, `zh-TW`/`zh-Hant-HK` use Traditional. Explicit incompatible scripts such as `hi-Latn` are not silently replaced.
- `node scripts/prepare-interface-packs.mjs zh zh-Hant th ar fa hi fr` generates validated fixed batches through the existing bounded public UI API. It resumes matching-revision checkpoints in `/tmp/ncg-interface-packs`, leaves incomplete languages unwritten and exits nonzero for incomplete results.
- Thai batch 4 returned 502 twice. Its 24 interface messages were translated by Codex, then checked for IDs, placeholders and links with the same validator; provenance is recorded in the Thai pack. All packs remain **unreviewed**, including this batch. Native-speaker review is still needed; these files are UI text, not authoritative Bible translations.
- Tests: 317 passed in 59 files; production build and 768-key catalogue check passed. For every generated pack, tests request every UI key and assert **zero calls to the translation endpoint**. Regional/script fallback behavior is also tested. Local Persian UI showed RTL layout with no horizontal overflow at 390px.

## Expanded static delivery — 2026-09-24 evening

Current catalogue: **748 UI messages**. Static delivery now covers **16 language/script combinations**: ko, en, es, pt, zh, zh-Hant, th, ar, fa, hi, fr, ja, vi, id, ru, sw. Japanese, Vietnamese, Indonesian, Russian and Swahili were authored with Codex; generated packs remain `reviewed:false`. Each of the 13 generated packs has every current key and a Korean source-context snapshot. Native-language/theological review is still pending. This change covers interface text, not Scripture editions, lesson bodies, user posts or new video descriptions.

- The picker warms only a focused/hovered static pack and waits for its download before switching the interface, avoiding an English-first screen. Dismissing a pending selection cancels its application. A failed static download can be retried.
- Local cached translations are retained per English key and Korean context across catalogue revisions. Changed contexts and malformed placeholders are rejected instead of invalidating all unchanged messages. Exact regional/script packs take priority, and fallbacks never cross writing systems.
- Build checks and tests discover **every** generated pack, enforce key/context completeness and preserve placeholders. Runtime tests read all current messages in each pack with **zero translation endpoint calls**.
- `node scripts/prepare-interface-packs.mjs --all --plan` inventories generation candidates without API requests. `--all` includes existing script packs and living non-sign-language ISO candidates; it is not a supported-language or translation-quality guarantee. At this checkpoint: 6,809 automatic-generation candidates, 13 complete, 6,796 pending, plus bundled ko/en/es.
- Generation preserves complete existing messages, resumes `tmp/interface-packs/` (Git-ignored), retries transient failures once, publishes only complete validated packs atomically, and stops globally on a quota/auth/revision error. The default run cap is 240 requests, maximum 300, and the shared server limit remains 300 uncached requests per UTC day. Unsupported results are remembered for the revision; explicit `--retry-unsupported` permits another attempt. Billing/security limits were not increased.
- Lao reached **312/748** validated messages before the server returned `429 translation_daily_limit`. The incomplete pack is kept in `tmp/interface-packs/lo.json` and is **not deployed**. Resume with `node scripts/prepare-interface-packs.mjs lo --max-requests=60` after the shared daily counter permits it. Do not claim all 7,867 selectable ISO entries are translated. No unattended generation job was started.
- Increasing language assets exposed an existing offline error: the built shell already exceeded the old 100-file validation cap. The bounded manifest now allows up to 1,024 files / 32 MB, and online runtime eviction uses a separate cache so it cannot delete the verified offline download.
- Validation: **74 files / 379 tests passed**, TypeScript and production build passed. All five new packs contain exactly 748 current keys with preserved placeholders/links. Local browser Japanese and Russian switching succeeded; Japanese, Russian and Arabic (RTL) at 390px with large text had no document overflow after wrapping the longer Russian QT button. Device emulation is not native-phone verification. The offline build contains 332 files / approximately 5.9 MB; language chunks stay lazy-loaded online.
